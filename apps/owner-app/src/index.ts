// Public surface of the owner app (M29 / D13) — the executive brief model: the
// day's numbers, the three things needing attention, grouped alerts that drill to
// their transactions, and the approvals inbox. Pure and deterministic, so it renders
// with the AI narrative off and shows freshness honestly.

export * from './brief';
export * from './owner-session';
