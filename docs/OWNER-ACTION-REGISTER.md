# Owner Action Register

**Purpose.** One consolidated list of the decisions and actions only the owner can take — legal
authorizations, paid vendor selections, production credentials, and irreversible GO decisions. The
build does **not** stop waiting for these; each row names exactly what is blocked and what continues
without it. Recommended defaults are given so a decision is a yes/no, not an open question.

**Last updated:** 18 August 2026 (**OA-14 ANSWERED** — the owner chose to **keep the repository PRIVATE and
UNLICENSED** (all-rights-reserved) for the pilot and to **add a `CODEOWNERS` naming him** as the required
reviewer. Implemented: `.github/CODEOWNERS` (`* @rchezhian81-prog`); no `LICENSE` added (a deliberate
all-rights-reserved posture, not an invented licence); repo visibility left private and unchanged. The one
remaining step is a repo admin toggle only the owner can flip — **Settings → Branches → Require review from
Code Owners** — which makes that approval blocking; documented in `docs/runbooks/branch-protection.md`.
Earlier: **OA-11 ANSWERED (partly)** — the owner set delivery to **the main store,
within 10 km, 8 slots a day**. The slot-generation engine is built and tested; delivery hours and per-slot
capacity are defaulted to 9 am–9 pm / 10-per-slot (overridable), and the ONE remaining fact is the store's
map coordinates (latitude/longitude) for the 10 km distance check — no safe default exists, so delivery
stays fail-safe (switched off, never a guessed 0,0) until they are given. Earlier: **OA-13 ANSWERED and OA-4 pilot posture confirmed** — the owner chose
**guest browsing** for the pilot and to **keep payment in test mode**. Investigating that decision
corrected a wrong earlier premise: the customer app is NOT engine-only — it is already a served,
offline-first, **guest-by-design** storefront fed the published catalogue, integration-tested (it opens a
real catalogue and search works), offline- and honesty-guarded, bilingual. So guest browse + cart review
(M20-FR-01/02) are WIRED today; the M20 record has been corrected from "ENGINE ONLY" to "PARTIALLY WIRED".
What still waits: full checkout needs delivery slots (OA-11) and — only for real money — a payment
provider (OA-4; test mode covers the pilot). OA-13 is therefore closed. Earlier: **OA-12 added** — the plans/pricing model, M36 commercial decision: the
entitlement CONTROL PLANE is wired (a tenant's optional modules are default-off, manageable and readable),
but the PAID-PLAN tier — priced plans, what each plan grants, metered ceilings, and suspend-for-non-payment
— cannot be built until the owner decides the commercial model. Earlier: **OA-11 added** — the delivery serviceability model, AVR-13: M18-FR-03
order-routing is a built, tested engine but cannot be wired to the live API as a usable feature until the
owner supplies the radius/slots/dark-store/express figures it consumes; these are genuine business
decisions, not values the build may invent. Earlier: **OA-10 added** — a low-risk, non-blocking request
for a native-Tamil review of the Warehouse handheld's on-screen wording; the words are present, complete
and guarded, and the English is authoritative, so this is a quality check before go-live, not a blocker.
Earlier:
**OA-9 CLOSED** — the owner approved the standard **dual-interface
warehouse** design: an offline-first, scanner-first **Warehouse PWA** for execution and a **Web ERP**
supervisory/admin surface, both on the same authoritative warehouse/inventory services with no
duplicated business logic. Build backend integration first, then the two surfaces. See the OA-9 note
below. Earlier: added OA-9 as an open decision; created during the project-recovery kickoff after the
read-only audit and the Phase 1 RBAC repair).

## OA-9 decision (owner-approved, 8 August 2026) — dual-interface warehouse

The owner closed OA-9 with the standard dual-interface warehouse design (this settles the offline-pack
vs cloud-screen question in favour of BOTH, cleanly separated):

1. **Warehouse execution is a dedicated, offline-capable Warehouse PWA (mobile/scanner-first):** barcode/QR
   scanning; receiving & GRN; put-away suggest & confirm; bin-to-bin & warehouse transfers; replenishment;
   cycle counts & stocktake; damaged/expired/recalled/quarantined handling; exception capture & supervisor
   escalation.
2. **Web ERP provides supervisory/administrative functions:** warehouse/bin configuration; stock
   visibility; transfer planning; task assignment; approvals; exception queues; discrepancy investigation;
   reports/KPIs/audit history.

**Both surfaces use the SAME authoritative warehouse/inventory services and controls — no duplicated
business logic.** Cross-cutting requirements: English + Tamil; role-based access + tenant isolation;
offline queue + safe synchronization; large touch targets, minimal typing; scan confirmation with
visual/sound/vibration feedback where supported; prevention of wrong-bin / wrong-SKU / duplicate-scan /
unavailable-stock movements; FEFO/expiry/recall/quarantine enforcement; reason codes + approval for
adjustments; complete audit trail + idempotency; accessibility + responsive-design verification.

**Execution order (owner-directed):** finish the M09 **backend** first (FR-02 replenishment, FR-03
transfers, FR-04 counts — the authoritative services the two surfaces will share), then build the
**Warehouse PWA** and the **Web ERP** supervisory surfaces against those services. Proceed autonomously;
no further technical approval required.

| # | Decision required | Recommended default | Alternatives | Cost / risk if delayed | Required-by | Work BLOCKED until decided | Work that CONTINUES regardless |
|---|---|---|---|---|---|---|---|
| **OA-1** | **Provide the controlling roadmap file `v2.1` ("Final Audit-Closed Developer-Ready Baseline").** The repo currently holds only `roadmap-v2.0.docx`. | Upload the v2.1 `.docx` to `docs/roadmap/`; we record it as controlling and keep v2.0 as superseded. | Confirm in writing that v2.0 **is** the controlling baseline and v2.1 is a naming difference. | Low today — v2.0 and v2.1 are described as the same scope. Risk is auditing/building against a stale baseline if v2.1 added requirements. | Before the next release gate | Any requirement that v2.1 adds or changes beyond v2.0 | All Phase 1–3 assembly (v2.0 scope is unchanged and sufficient) |
| **OA-2** | **EX-02 — lawful export access to the incumbent ERP** (credentials or a signed data extract). | Request full read-only export + a signed extract from the current vendor now. | Owner-supervised on-site extraction; or vendor-provided backup. | High and time-sensitive: real migration cannot be verified until real data is seen; unknown data faults stay invisible until cutover. | Before any cutover / go-live | Real full-history migration; parallel run; final cutover | Migration tooling, reconciliation, trials against synthetic/anonymized fixtures (Phase 8) |
| **OA-3** | **AI model provider** selection + terms + API keys. | Defer until the governed runtime is ready for live evaluation; keep the simulator. | Pick a provider now for early evaluation. | Low — AI is advisory-only and off by design; no trading depends on it. | Before enabling live AI | Live-model AI evaluation and any real agent output | Full governed AI runtime against the simulator (Phase 9) |
| **OA-4** *(pilot posture confirmed 8 Aug 2026 — KEEP TEST MODE for the pilot; real providers still needed before production)* | **Payment tokenization provider** + **production identity provider (IdP)**. | Select an India-market payment provider and an OIDC/SAML IdP; provision `IDP_*` secrets. | Continue with the standards-compliant local/test IdP adapter for the pilot. | Medium — real tender and real login need these; the pilot can run in tender test-mode + test IdP. | Before production; test-IdP suffices for pilot | Real card/UPI settlement; production single-sign-on | POS pilot (test IdP + tender test-mode), all wiring behind ports/adapters (Phase 7) |
| **OA-5** | **Hosting region + production credentials + spend approval** (India-region deployment). | Approve an India-region managed host + budget; issue production secrets to a secrets manager. | Self-hosted on owner infrastructure. | Medium — no production deploy without it; DR/residency depend on region. | Before production | Production deployment, DR drills against production topology | Infra-as-code, staging, and all readiness proofs (Phase 10) |
| **OA-6** | **Genesis owner identity per tenant** — the userId (from the IdP) who becomes each store's first owner/administrator. | Provide the owner's IdP subject id; we seed it once via `BOOTSTRAP_OWNER_*` config. | Provision the initial admin set through an operator runbook. | Low but blocking for that tenant: authorization is now real and fail-closed, so an unprovisioned tenant can do nothing (by design). | Before that tenant's first use | First login/authority for a specific tenant | The RBAC mechanism itself (shipped and tested); other tenants |
| **OA-7** | **Trading-day cut-off time (A-13)** and other open AVR/owner decisions in the roadmap control table. | Confirm the store's cut-off (e.g. `00:00` default or a small-hours value). | Leave default `00:00` for the pilot. | Low — default is safe; wrong cut-off dates late-night sales to the wrong business day. | Before finance go-live | Correct trading-day dating in finance/reports | Everything else (default applies meanwhile) |
| **OA-8** | **Final pilot GO** and, separately, **production GO**. | Give pilot GO once the Phase 11 pilot conditions are green; production GO later against the full checklist. | — | — | At each gate | Pilot start; production start | All build work up to each gate |
| **OA-13** ✅ **ANSWERED 8 Aug 2026 — GUEST BROWSING for the pilot.** Investigation on answering found the customer app is already a served, offline-first, guest-by-design storefront (no login), fed the published catalogue and integration-tested — so guest browse + cart review (M20-FR-01/02) are already WIRED, and the M20 status was corrected to PARTIALLY WIRED. Full checkout still awaits delivery slots (OA-11); accounts/OTP are deferred. | **Customer identity/authentication model for the M20 storefront (customer app / web store).** M20 is the last engine-only module: the shopping-session, cart-review-against-live-catalogue, slot booking and place-order engines are all built and tested (`apps/customer-app/src/`, `packages/storefront/`). What blocks wiring them to the live API is not those engines but WHO a customer is: the cloud API authenticates and authorises every request against the STAFF role catalogue, and a shopper is not a staff member — there is deliberately no unauthenticated/public route class (every route declares a permission, default-deny holds at the router). So a customer-facing surface needs a customer-principal decision that only the owner can frame commercially. **Note:** an OTP account needs an SMS/OTP provider — an external dependency (OA-4 territory). | Decide the customer access model: (a) **guest browsing** (catalogue/search with no account; identify only at checkout via order reference + phone) — smallest, and it defers OTP; or (b) **OTP accounts** from the start (saved lists, favourites, order history) — needs an OTP provider. We then wire the chosen customer-principal path and the guest-catalogue read on top of the already-wired catalogue/price truth. | Start with guest browse for the pilot and add accounts later; or defer the whole customer app until after go-live and focus on in-store + back-office. | **Low today** — the storefront engines are built and tested; nothing in-store or back-office depends on this. It blocks only the public-facing app. | Before the customer app goes live | The customer app / storefront API (browse, cart, checkout, account) | Everything else — in-store, warehouse, back-office, platform; the storefront ENGINES (built and tested) |
| **OA-12** | **Plans & pricing model for the multi-tenant platform (M36-FR-01 paid-plan tier).** The entitlement control plane is built and wired: a tenant's optional modules (delivery, b2b, loyalty, customer app, the conditional departments) are default-off, can be turned on/off per tenant, and the authoritative default-deny decision is exposed through the `checkEntitlement` engine. What is NOT built is the COMMERCIAL layer that engine also supports: named priced plans, which modules each plan grants, metered ceilings per dimension (lanes/users/transactions/storage), overage pricing, and suspend-for-non-payment. Those are commercial decisions (OB-01 / ADR-0003), not values the build may invent, and the whole tier is **R8** unless prioritised sooner. Note: by design a metered limit is invoiced but NEVER stops a shop trading (P-01). | Defer — keep entitlements as explicit per-tenant grants (what is wired) for the pilot and single-tenant operation; decide plans/pricing when commercialising to other retailers. | Provide an initial plan catalogue now (plan names, per-plan module grants, ceilings, overage rates) and we wire the plan-based entitlement + metering-to-invoice tier on top of the existing control plane. | **Low** — SRE runs as a single tenant with everything it needs enabled; the paid-plan tier only matters when selling to other retailers. | Before commercialising to other tenants | Plan-based entitlement grants, metered billing, suspend-for-non-payment | The entitlement control plane (default-off enable/revoke/read + decision), tenant isolation, export and branding — all wired |
| **OA-11** *(ANSWERED 8 Aug 2026 — main store, within 10 km, 8 slots/day; slot engine built; hours/capacity defaulted to 9am–9pm / 10-per-slot, overridable; store map coordinates still needed to switch delivery on)* | **Delivery serviceability model — the AVR-13 open item (radius, slots, dark-store, fleet).** M18-FR-03 order routing (`packages/orders/src/fulfilment-plan.ts`) is a complete, tested engine: it routes pickup/scheduled/express across stores and dark stores by distance, live stock and slot capacity, refuses an unachievable express promise, and flags an unprofitable drop without blocking it (D09). It cannot be wired to the live API as a **usable** feature until the owner supplies the SERVICEABILITY DATA the engine consumes — which locations serve delivery, each one's radius (the "10-km configurable" figure, D08), slot capacities per method, express achievable-minutes, and which sites are dark stores. These are genuine business decisions (AVR-13), not values the build may invent (CLAUDE.md: never invent a requirement). | Provide, per branch/dark-store: delivery on/off, radius in km, delivery/pickup slot capacities, express achievable-minutes, and the dark-store list — we wire the routing surface to those figures. | Nominate a single pilot branch with one radius and one slot size; defer multi-site/dark-store/express to a later release. | **Low today** — no channel is taking online delivery orders yet, and the routing engine plus the order lifecycle/reservation are built and tested. Blocks turning on delivery routing. | Before delivery / online-order go-live | Wiring M18-FR-03 routing + slots to API-07 as a usable feature; M20-FR-03 delivery slots | Order lifecycle, reservation/ATP and cancellation-release (all wired on API-07); the routing engine itself (built and tested) |
| **OA-10** | **Native-Tamil review of the Warehouse handheld's on-screen wording (OA-9 bilingual requirement).** The Warehouse PWA shows every scan result — received, put away, and every refusal (unknown barcode, over-delivery needs approval, wrong bin, bin full, recalled/expired into a pickable bin, etc.) — in English and Tamil. The English is authoritative; the **Tamil was written by the build and needs a native speaker to confirm it reads correctly to a warehouse worker** before go-live. A completeness check already fails the build if any word is missing in either language, but it cannot judge whether the Tamil is *right*. | Have a Tamil-speaking staff member or the owner read the warehouse Tamil words — the handheld scan-feedback (`apps/warehouse-app/web/app.js`, `ta:` block) AND the supervisor screen (`apps/web-erp/web/warehouse.js`, `ta:` block: exception names and section headings) — and correct any that are unclear; we apply the corrections. | Accept the current Tamil for the pilot and refine from worker feedback on the floor. | **Low** — the words are present and complete, the English is correct, and every refusal also carries a colour and a sound; the risk is only that a Tamil phrase reads awkwardly. Not trading-blocking. | Before warehouse go-live (pilot can run and gather feedback) | Confident Tamil wording on the warehouse handheld | The whole Warehouse PWA (engine, feed, shell, offline, scan feedback) and its English wording — all shipped and tested | 
| **OA-14** ✅ **ANSWERED 18 Aug 2026 — PRIVATE + UNLICENSED, `CODEOWNERS` naming the owner.** Implemented `.github/CODEOWNERS` (`* @rchezhian81-prog`) so the owner is auto-requested on every PR; no `LICENSE` added (deliberate all-rights-reserved, not an invented licence); repo left private and unchanged. **Remaining owner step (admin toggle):** enable *Require review from Code Owners* in Settings → Branches to make that approval blocking — see `docs/runbooks/branch-protection.md`. | **Repository governance posture — licence, visibility, code ownership.** The Phase 0 baseline-recovery pass audited the repo's governance and found three items that only the owner can settle: (1) there is **no `LICENSE` file** and `package.json` carries no `license` field (it is marked `"private": true`) — the code is currently all-rights-reserved by default, which is fine for a private build but must be a deliberate choice; (2) there is **no `CODEOWNERS`** file, so no required reviewer is auto-assigned; (3) repository **visibility** (private vs public) is an admin setting the build does not touch. CI (`.github/workflows/ci.yml`) is present and `main` is branch-protected (`docs/runbooks/branch-protection.md`). | **Keep the repo private and unlicensed (all-rights-reserved) for the pilot** — the safe default for a single-owner commercial build; add a `CODEOWNERS` naming the owner (or a trusted reviewer) as the required approver so every PR has an accountable sign-off. | Choose and add an explicit licence (e.g. a proprietary EULA, or an open-source licence if the owner intends to share the code); or make the repo public once a licence is chosen. | **Low** — the repo is private and protected today; nothing is exposed. The risk is only an implicit/unstated licensing position and no named code owner. | Before any external code sharing or additional contributors | An explicit licence; public visibility; auto-assigned required reviewers | All development, CI, branch protection and PR flow (already in place) |
| **OA-9** ✅ **CLOSED 8 Aug 2026** | **First on-screen (office) increment — where the new back-office screens live.** Dozens of back-office jobs are now built and tested on the cloud (customer credit, collections/ageing/dunning, salesperson commission, supplier statements, facilities & compliance, waste, integration health). **None yet has a screen.** The store's 16 existing screens are all *offline-first* — they keep working with no internet because they are fed from a data pack kept **inside the store**. The question is where the *new* office data belongs. | **Keep the in-store pack for what the shop floor needs to trade; put pure office data (a compliance report, a supplier statement, integration health) on a separate office screen that runs from the cloud.** Start with one office screen over a job already built (e.g. the supplier statement, or B2B collections). This is the smallest honest first screen and it respects *why* offline-first exists — the till must trade with no internet, and a compliance report is not a trading input. | **(a)** Improve an existing offline screen with a figure it is already fed — smallest of all, but it does not answer where office data goes. **(b)** Build cloud→store sync for one office dataset and show it on an existing offline screen — largest; it makes that data available with no internet, at the cost of a new sync path and a heavier in-store pack. | **Low.** The cloud jobs work now; the only thing waiting is which screen pattern to start with. Guessing risks building the first screen the wrong way and redoing it. | Before the first on-screen increment | The **first office screen** (which pattern, and whether office data enters the offline pack) | All remaining cloud wiring, engines and tests (this decision changes the *screen*, not the data or the rules underneath it) |

## Notes

- **These are the only things the build waits on.** Everything else proceeds autonomously per the
  execution plan (Phases 0–11).
- **OA-1** is a document-control gap, not a licence to invent content: no requirement will be added,
  removed, or altered on the basis of a v2.1 we have not seen. If v2.1 changes scope, those deltas
  become new tracked requirements once the file is provided.
- Owner decisions are recorded here and, when they change scope or release, also in
  `docs/registers/changes.md` per the roadmap's change-control rule.
