{
  "baseline": {
    "version": "v1",
    "date": "2026-08-13",
    "denominator": 104,
    "denominatorSource": "roadmap v2.0/v2.1 controlling structure: M01-M36 (36) + D01-D14 (14) + A01-A10 (10) + WF-01..20 (20) + QG-01..12 (12) + MG-01..12 (12). See docs/COMPLETION-MODEL.md.",
    "statusSource": "docs/traceability.md (RTM) + docs/STATUS.md, read conservatively (lower status when ambiguous) on 13 Aug 2026",
    "previousProductCompletionPct": 40.3,
    "note": "First computation — this is the baseline, so previous == current and change == 0. Established alongside the reporting model; carries no new product maturity itself."
  },
  "items": [
    {"id": "M01", "label": "PARTIALLY_WIRED", "evidence": "RTM: trading-day/config/number-series wired (M01-FR-02). ORG HIERARCHY now WIRED too (M01-WIRE, M01-FR-01): the company->GST->branch->warehouse->department skeleton on API-01 running the tested packages/org engine -- POST /v1/org/gst-registrations/:gstin checksum-validates a GSTIN (422 gstin_invalid on a bad check digit) and refuses a duplicate (409 gstin_already_registered, holder named), POST /v1/org/nodes/:nodeId upserts a node (a structural violation refused 422 hierarchy_violation; an incomplete node recorded as a DRAFT with its activation blockers reported; creating one already active requires it be activatable), POST /v1/org/nodes/:nodeId/activation activates only when canActivate (a branch needs a company + a valid OWN GST registration else 409 cannot_activate with reasons), GET reads a node's scope (ancestryOf/descendantsOf/gstinFor -- which GSTIN a transaction here is filed under) and the whole structure; gated platform.setup.write (manage) / org.branch.read (read); event-sourced OrgNodeSet/OrgGstRegistered, restart-safe, integration-tested 5 cases. HELD at PARTIALLY_WIRED: document templates (M31) and org-scoped report roll-ups remain follow-ons."},
    {"id": "M02", "label": "WIRED", "evidence": "RTM: WIRED, per-tenant authorization repaired 7 Aug; approval delegation FR-03 remainder now wired on the cloud (API-01: grantDelegation/effectiveAuthority/reviewDelegations/revoke — the honest alternative to the shared login, time-boxed + capped + unchained + separately authorised, decisions in the delegate's own name; event-sourced, restart-safe), tests/integration/approval-delegation.test.ts; and the §28 decision itself now wired too (POST /v1/access/approvals/decide runs decideWithDelegation — decider is always the authenticated caller, self_approval_forbidden + delegation_to_maker_forbidden enforced, returns the attributed decision but commits nothing, gated approvals.delegation.read), tests/integration/approval-decide.test.ts, so the whole packages/approvals decision surface is now on the API with nothing engine-only left; emergency access FR-04 (SEC-11) now wired too (API-01: grantEmergencyAccess/revokeEmergencyAccess/emergencyAccessReview — time-bound at grant + self-expiring + specific reason + separate approver §28 + capped + never extended in place + reviewable, event-sourced EmergencyAccessRecorded restart-safe), tests/integration/emergency-access.test.ts; joiner/mover/leaver leg now wired too (API-01: applyLifecycle decision — a mover replaces scope + closes sessions, a leaver blocked until owned open items reassigned then revoked + priority sync, caller is approver §28 self_service_access_refused; pure compute), tests/integration/access-lifecycle.test.ts"},
    {"id": "M03", "label": "WIRED", "evidence": "RTM: catalogue pack/read + category-policy wired; product duplicate-detection wired (POST /v1/catalogue/products/duplicates); product-master WRITE PATH wired (POST /v1/catalogue/products/:id/publish behind the compliance gate + GET reads, event-sourced latest-per-id, integration-tested); barcode register WRITE PATH wired (POST /v1/catalogue/products/:id/barcodes/:code enforcing one-code-one-item via the tested BarcodeRegistry + lookup/list reads, event-sourced, integration-tested); tax-class GST-RATE SCHEDULE wired (POST /v1/catalogue/tax-classes/:hsn/rates/:date via the tested resolveGstRate, per-HSN effective-dated, one-rate-per-date, + resolve/list reads, integration-tested); catalogue-pack ASSEMBLY + SIGNED PUBLISH now wired (GET /v1/catalogue/pack/preview folds product master + prices + barcodes + tax rates into one snapshot for a store, AND POST /v1/catalogue/pack now BUILDS the signed pack from that same fold — an authored+priced+barcoded+taxed product reaches the lane on the signed pack it reads, an unpriced one is never shipped; integration-tested end to end incl. the shrink guard); product-authoring SCREEN write path underway (slices 1-2: the tested ProductPublishRequested outbox command + the shell Publish button now commits it to a DEVICE-backed catalogue outbox via catalogue-session.requestPublish — Save validates then durably queues, no network from the screen; unit-tested); product-publish delivery per owner decision ADR-0013 (signed-in operator delivers, not a service identity) — slice 1 the tested review-queue classifier + slice 2 the tested operator-session delivery (deliverReadyPublishes sends only ready items via an injected operator-auth port to the idempotent publish route, acknowledging/holding/dead-lettering on result, no auto-publish) + slice 3 the tested review-queue SCREEN session model (createProductPublishReviewSession presents the whole outbox incl. visible dead-letters/published items, each with a live word+icon state re-evaluated from the operator's CURRENT context, attention-first ordering, plain-English exception guidance, bilingual EN/TA, a nobody-named refusal, and a SEPARATE deliver() action delegating to slice 2 — no auto-publish on view/login) + slice 4 the SHELL + edge wiring (web/product-publish-review.html+.js opens offline via the shared web-erp SW, draws rows + a Publish button that runs ONLY on an explicit click; browser-entry boots it over the SAME device-backed catalogue outbox the Save commits to, with a concrete operator-session delivery port POSTing to the idempotent publish route under credentials:'same-origin' — the operator's own auth cookie, never a service token; registered as a served screen with PackProductPublishReviewPolicy feeding only the operator's CURRENT context re-read each render, menu gated catalogue.pack.publish; usability guardrail + offline e2e + screen-feed tests) + slice 5 the CENTRAL-BOUNDARY re-checks + audit (the cloud publish route trusts no client verdict: re-runs the tested publishProduct compliance gate AND adds SKU uniqueness across the whole tenant — a SKU held by a DIFFERENT product is refused 409 sku_already_in_use, nothing overwritten, while same-product re-publish is a legitimate new version; audit attribution [ADR-0013 control 10] is already provided by the kernel — a SHA-256-sealed append-only audit_log row per write and per refusal, naming the AUTHENTICATED userId, FND-02) + slice 6 the BARCODE-ASSIGNMENT leg of delivery (the command carries each barcode as {code,kind} — the symbology is a real authoring fact, never guessed; deliverOnePublish publishes the product FIRST then assigns each barcode on the one-code-one-item route under the operator's own session, collapsing to one outbox status — a real barcode conflict dead-letters, a transient failure holds+retries idempotently, all-in acknowledges; also corrected the delivery classifier so a 409 is a REAL conflict → dead-letter, not a mistaken idempotent-success) + slice 7 the BROWSER E2E MATRIX (a real headless-Chromium test seeds the device outbox with a queued publish, injects the operator context, and drives the actual screen: an authorised operator's Publish sends the product-master POST then the barcode POST and the queue clears; a no-permission operator's button is disabled and nothing is sent; a 409 barcode conflict dead-letters — the ADR-0013 delivery chain is now fully E2E-verified end to end; this first real browser RUN also caught + fixed a defect the CI-skipped e2e had hidden — product-publish-review.js was missing from the service-worker SHELL cache so the screen did not truly open offline, now added + cache bumped v13→v14) landed; the §28 REVERSIBLE MERGE-WRITE is now WIRED too (M03-WIRE inc-A: POST /v1/catalogue/merges/:id PROPOSES [catalogue.merge.propose], POST …/decision APPROVES-or-rejects [the distinct catalogue.merge.approve, mirroring the price propose/approve split] with the tested mergeProducts refusing a self-approval on two SERVER-attributed identities so proposer≠approver per §28 even for an owner holding both codes, POST …/reverse undoes it as a reversible LINK never a deletion [hard rule #2], GET /v1/catalogue/products/:id/canonical resolves a merged id via resolveProductId; event-sourced MergeProposed→MergeApproved/MergeRejected→MergeReversed [rejections/reversals kept, hard rule #6], integration-tested 9 cases incl. a cold-restart rebuild). The SECOND tested engine is now WIRED too (M03-WIRE inc-B): POST /v1/catalogue/products/:id/pack DEFINES a product pack ladder (unit->inner->case) behind the tested validatePack — an inexact pack (non-integer count of the level below, base level != one base unit, two levels sharing a name, or none) is refused 422 pack_hierarchy_is_not_exact at DEFINITION time, before it can make a stock figure wrong at the back door; GET .../pack reads it; GET .../pack/convert?level=&quantity=&direction=to-base|from-base runs the tested toBaseUnits/fromBaseUnits/conversionIsReversible (2 cases -> 48 base units exactly; 50 base units -> 2 whole cases + 2 singles, never a fraction of a case); event-sourced PackHierarchyDefined latest-per-product [hard rule #2], integration-tested 8 cases incl. a cold-restart rebuild. RE-RATED to WIRED (owner-ratified Option A, 19 Aug): both ENGINE_ONLY M03 engines the re-assessment named — reversible merge (inc-A) + pack/UOM conversion (inc-B) — are now live on the API, on top of the already-wired+integration-tested product-truth core (FR-01 authoring, FR-02 barcode identity, FR-03 tax/MRP/allergen/recall) and the E2E-verified ADR-0013 operator-publish chain. Remaining M03 follow-ons (NOT blocking WIRED, the path onward to INTEGRATION_TESTED/E2E for the whole module): images + bulk-edit (FR-04), a category-delete-with-stock guard, and EmbeddedBarcodeRule weight/price-embedded parse (D01-attributed, a separate denominator item)."},
    {"id": "M04", "label": "PARTIALLY_WIRED", "evidence": "RTM: picking PARTIALLY WIRED (app-shell); shelf-count producer wired on the cloud (API-04 blind, append-only record + latest + staleness-aware worklist, server-attributed counter), tests/integration/shelf-count.test.ts; planogram-compliance CONSUMER wired (recorded counts drive refill-vs-reorder tasks, never-counted excluded from % — P-08), tests/integration/planogram-compliance.test.ts; space + display-contract governance FR-04 wired (API-04: spacePerformance ranks areas by MARGIN per sq ft with not_meaningful vs a fabricated zero and flags margin-share-below-space-share; display contracts recorded append-only and reviewDisplayContracts flags expired-still-occupying / unapproved / funding-not-received — §28/M23), tests/integration/space-and-display.test.ts; assortment/range FR-01 wired (API-04: list/drop — a drop with stock on hand routes to CLEARANCE not a silent delete via dropFromRange, reason+decider mandatory, effective-dated; checkAssortmentIntegrity flags sold-not-in-range / reordered-not-listed / clearance-with-no-stock / listed-never-sold; range read resolves as-at a date), tests/integration/assortment.test.ts. ALL FOUR M04 FR engines now wired on the cloud; the one remaining piece — a persisted planogram/shelf-map store (the compliance route takes the plan in the body) — is owner-DEFERRED to R6 (CH-02, docs/registers/changes.md, 24 Aug 2026); deferred-but-unbuilt, so M04 stays PARTIALLY_WIRED (headline not inflated by the deferral)"},
    {"id": "M05", "label": "WIRED", "evidence": "RTM: WIRED, all four FRs integration-tested. FR-04 post-launch measurement now also wired (was the last engine-only tail of packages/promotions/src/simulation.ts): POST /v1/promotions/:id/effectiveness runs measureEffectiveness (verdict is incremental MARGIN not units — 'busier and poorer' is worthDoing:false; supplier funding can turn it positive) and POST /v1/promotions/:id/vendor-funding runs reconcileVendorFunding (claimed vs received, an outstanding balance named), both pure computes gated promotion.read, integration-tested (tests/integration/promotion-effectiveness.test.ts, 5 cases). And the abuse cap now wired too: POST /v1/promotions/:id/abuse-check runs checkAbuseLimit from counts the caller supplies, so the SAME cap runs at an offline lane off its cached pack (§31, P-01) and returns countMayBeStale when offline (P-08); decides only, gated promotion.read, tests/integration/promotion-abuse-check.test.ts (5 cases). Simulate/launch/price-list/promotion-catalogue were already live; the whole packages/promotions/src/simulation.ts engine is now on the API with nothing engine-only left; M05 stays WIRED."},
    {"id": "M06", "label": "WIRED", "evidence": "RTM: supplier/procurement — bank-change verification recorded as an event (D03-FR-06 wired). PO ISSUE + SUPPLIER-BLOCK + OPEN-COMMITMENT now WIRED on API-03 (M06-WIRE inc-1, M06-FR-01/02/04): POST /v1/purchase/orders/:poId PROPOSES a PO (gated purchase.order.propose, requisitioner = the authenticated buyer), POST .../approval APPROVES+ISSUES it via the tested decide()+issuePurchaseOrder — the approver cannot be the requisitioner (§28 enforced by identity even when one user holds both codes → 409 proposer_cannot_approve) and a blocked supplier is refused (409 supplier_blocked); the hold is its own append-only latest-wins record (POST /v1/purchase/suppliers/:id/block-status, gated purchase.supplier.block); GET /v1/purchase/commitments now runs the tested computeOpenCommitment over the issued POs, flipping from not-known to a real figure (still not-known, not zero, until the first PO exists); event-sourced PurchaseOrderProposed/PurchaseOrderIssued/SupplierBlockStatusSet, restart-safe, integration-tested 6 cases. SUPPLIER SCORECARDS + CONTRACT ALERTS now WIRED too (M06-WIRE inc-2, M06-FR-03): POST /v1/purchase/suppliers/:supplierId/receipts/:poId records a delivery OUTCOME per PO (gated purchase.performance.record, latest-per-PO so a correction supersedes and the score counts a delivery once), POST /v1/purchase/contracts/:contractId records a contract (gated purchase.contract.manage, latest-per-id), GET /v1/purchase/suppliers/:supplierId/scorecard runs the tested scoreSupplier over the recorded facts + the active contract (fill rate, on-time vs contracted lead time, lead-time RELIABILITY from the spread, price adherence, quality, weighted overall, worst-signal-first summary, not_rated where there is no evidence), GET /v1/purchase/contracts/alerts runs reviewContracts (expiring/expired/UNAPPROVED worst-first); event-sourced SupplierReceiptRecorded/SupplierContractRecorded, restart-safe, integration-tested 7 cases. PO AMEND/CANCEL/RECEIPT + open-commitment NETTING now WIRED too (M06-WIRE inc-3, M06-FR-04): POST /v1/purchase/orders/:poId/amendments amends an issued PO's lines (gated purchase.order.approve, keeps the prior lines on the ledger, re-prices), .../cancellations records an approved per-product cancellation, .../receipts posts received qty per product (gated purchase.order.receive, in the PO's ordering units); computeOpenCommitment now nets ordered − received − cancelled and GET /v1/purchase/commitments follows; amend/cancel/receive refused on a non-issued PO (409 purchase_order_not_issued), each idempotent on its own id (no double-count); event-sourced PurchaseOrderAmended/PurchaseOrderCancelled/PurchaseOrderReceiptPosted, restart-safe, integration-tested (12 cases total). REBATES now WIRED too (M06-WIRE inc-4, M06-FR-03 / M23): POST /v1/purchase/rebate-schemes/:schemeId records a scheme (gated purchase.contract.manage, latest-per-id), POST .../accruals/:accrualId runs the tested accrueRebate (nothing accrues below threshold and it says how far short, a growth scheme measures against baseline, outstanding = accrued − received is the earned-not-claimed headline, latest-per-accrualId so a re-measure/payment supersedes), GET .../accruals totals accrued/received/outstanding; event-sourced RebateSchemeRecorded/RebateAccrued, restart-safe, integration-tested 6 cases. REQUISITION/RFQ/QUOTATION-COMPARISON now BUILT + WIRED (M06-BUILD inc-5 engine + inc-6 wiring, M06-FR-02): the tested compareQuotes (packages/purchasing/src/rfq.ts) ranks quotes like-for-like — cheapest + fastest per line and overall, an incomplete or different-currency quote shown but never ranked, only a quote covering every line totalled (lead time = the slowest line); wired on API-03 — POST /v1/purchase/requisitions/:id raises a requisition, POST .../quotes/:quoteId records a supplier quote (latest-per-id, a re-quote supersedes), GET .../comparison runs compareQuotes, GET /v1/purchase/requisitions lists the worklist; gated purchase.order.propose (record) / purchase.commitment.read (read); event-sourced RequisitionRaised/QuoteRecorded, restart-safe, integration-tested (rfq.test.ts 5 cases + rfq unit 6). RE-RATED PARTIALLY_WIRED->WIRED: every M06 FR is now live on the API — FR-01 (supplier onboarding/bank-change/blocked), FR-02 (requisition->RFQ->quote-compare->PO issue under §28), FR-03 (contracts/scorecards/rebates), FR-04 (amend/cancel/receive + open-commitment netting). (Follow-ons NOT blocking WIRED: the finance-side rebate-claim APPROVAL (M23), a unit-reconciled PO<->GRN three-way join, and buyer-facing screens.)"},
    {"id": "M07", "label": "PARTIALLY_WIRED", "evidence": "RTM: FR-04 three-way match wired (7 Aug). GRN CAPTURE now WIRED too (M07-WIRE / D03-FR-02): POST /v1/inventory/goods-receipt/:grnId re-runs the tested captureReceipt at the boundary (FR-02/03) -- a batch-tracked line with no batch/expiry refused 422, damaged/QC-failed/expired quarantined or rejected and never made availability, short/excess/MRP raise valued discrepancies, over-tolerance excess needs a second person -- then turns the SELLABLE quantity of each line into availability (one inbound received movement per sellable line, GRN + movements ONE atomic append FND-01, unit cost carried for valuation M08-FR-04), idempotent on the GRN id so a re-scan/re-sync is one effect (§31.1); gated inventory.movement.append (receiver != price); GET reads open/approval-needed first. Integration-tested 7 cases incl. restart. And the richer three-way match now WIRED too (M07-FR-04/D03-FR-05): POST /v1/purchase/invoices/:invoiceId/reconcile runs matchInvoice -- the full invoice-vs-PO-vs-receipt match that values every variance, apportions landed cost (freight/duty) across the lines to the paisa, and blocks out-of-tolerance payment until a non-receiver approves (§28); stateless, records nothing, gated purchase.invoice.match, distinct from the store-backed /match, tests/integration/purchase-invoice-reconcile.test.ts 5 cases. And the back-door dock scheduling + ASN comparison now wired too (M07-FR-01): POST /v1/inventory/dock-slots/book runs bookDockSlot (an overlap on the same door refused 409 dock_conflict; a cancelled/no-show slot frees it) and POST /v1/inventory/asn/compare runs compareAgainstAsn (only the differences -- the advice note is a promise not a receipt -- for the discrepancy path), both stateless, gated inventory.movement.append / inventory.availability.read, tests/integration/inventory-asn-dock.test.ts 5 cases. HELD at PARTIALLY_WIRED (conservative): still edge-only / not on the cloud -- dock scheduling + handheld scan surfaces (warehouse PWA), the discrepancy-DISPOSITION approval write path (accept/return/claim -- the GRN flags requiresApproval but the decision is not yet a route), and OCR invoice ingestion (D03-FR-02 OCR)."},
    {"id": "M08", "label": "INTEGRATION_TESTED", "evidence": "RTM: INTEGRATION TESTED, valuation/ageing/performance wired — and stockout-impact now wired too (POST /v1/inventory/stockout-impact runs stockoutImpact, an estimate never mixed into actuals; gated inventory.availability.read, tests/integration/inventory-stockout-impact.test.ts 5 cases), so the whole packages/stock/src/metrics.ts stock-health surface (ageing/turns/GMROI/stockouts) is now on the live API with nothing engine-only left"},
    {"id": "M09", "label": "WIRED", "evidence": "RTM: WIRED, all four FRs integration-tested"},
    {"id": "M10", "label": "WIRED", "evidence": "RTM: recall->POS + cold-chain (FR-02, built+wired 11 Aug) + lot-trace (FR-03) wired; FEFO expiry action list wired (POST /v1/inventory/expiry-actions); FEFO ALLOCATION wired (POST /v1/inventory/fefo-allocation, M10-WIRE inc-1, earliest-expiry-first + honest shortfall); the expiry/recall SCREEN exists (apps/web-erp expiry.* + edge-registered + offline, 22-case unit test). RECALL LIFECYCLE now WIRED too (M10-WIRE inc-2): the recall BLOCK reaches every lane on the signed pack (recalled product master -> recallBlock in the pack -> POS refuses even offline, guarded by a-recalled-tin-cannot-be-sold), and the recall RECORD is a durable cloud record -- POST /v1/quality/recalls/:batchId initiates (gated quality.recall.initiate, idempotent), POST .../closure closes ONLY with evidence (422 otherwise, 409 nothing-open-to-close), GET reads open-first; runs the tested RecallRegistry, event-sourced RecallInitiated/RecallClosed retained forever (hard rule #6), integration-tested 7 cases incl. restart. RE-RATED PARTIALLY_WIRED->WIRED: every M10 FR (FR-01/02/03/04) is now live on the API. (Follow-ons NOT blocking WIRED: an expiry-screen usability guardrail to match other back-office screens, and a dedicated offline e2e.)"},
    {"id": "M11", "label": "WIRED", "evidence": "RTM: WIRED, all four FRs integration-tested"},
    {"id": "M12", "label": "E2E_VERIFIED", "evidence": "RTM: E2E VERIFIED, offline commit+sync on real PostgreSQL. CORE-ONE-LANE (prove the core on one lane): proving the spine end-to-end through the REAL lane record — not a hand-built cloud-shaped one — surfaced a real break: the lane writes its sale to disk as {id,total,tender.amount.minor} with no packVersion, the cloud /v1/sales reads {saleId,totalMinor,packVersion,amountMinor}, and the outbox carried the disk record verbatim, so every rung sale was refused 400 and dead-lettered — money in the drawer, no record in the cloud (the prior DB-gated 'reaches the cloud' gate missed it because it fed the edge node a pre-mapped cloud-shaped record and skips without DATABASE_URL). Fixed with a translation at the edge sync boundary (edge/store-edge/src/cloud-sale.ts toCloudSale, stamping packVersion from the pack the edge holds), applied at both enqueue sites (createEdgeNode.commit + startEdge boot-restore). New ALWAYS-ON in-memory E2E rings the sale the way the lane actually does and drives the real edge+outbox+SyncAgent+httpTransport+cloud surface: offline-first (disk+queue, cloud untouched) -> banked -> owner dashboard shows the takings -> idempotent resend no double-count; plus a regression guard that the raw till record is still refused 400 while the translated one banks. tests/e2e/core-one-lane.test.ts, tests/unit/edge-cloud-sale.test.ts. HONEST SCOPE: proves ONE lane's happy path + offline + idempotency through the real wire; NOT UAT, NOT production."},
    {"id": "M13", "label": "PARTIALLY_WIRED", "evidence": "RTM: FR-01/03 refund guard wired; tender capture off-cloud"},
    {"id": "M14", "label": "PARTIALLY_WIRED", "evidence": "RTM: cash-office trio wired; edge trading-day lock outside"},
    {"id": "M15", "label": "WIRED", "evidence": "RTM: WIRED, all four FRs integration-tested"},
    {"id": "M16", "label": "PARTIALLY_WIRED", "evidence": "RTM: CRM/loyalty — points/consent/data-rights wired; consent-gated segmentation FR-02 wired (buildAudience needs profiling AND marketing consent, excluded count always reported; rankByValue by margin not revenue), tests/integration/customer-segments.test.ts; duplicate detection FR-01 now wired too (API-06: detectDuplicateCustomers — verified phone/email = high-confidence merge_candidate, unverified contact/matching name = low-confidence review, ordered high-first, NEVER auto-merged P-08/§28; stateless pure compute over the caller's candidate set), tests/integration/customer-duplicates.test.ts; the per-tenant segment-policy + profile persistence remain"},
    {"id": "M17", "label": "WIRED", "evidence": "RTM: points (FR-01) + gift cards/store credit (FR-03) + household pooling/cross-channel double-spend (FR-04) all WIRED+integration-tested; coupons/referrals (FR-02) NOW WIRED (M17-WIRE) — the tested redeemCoupon/issuePersonalisedOffer/assessReferral wired on API-06: POST /v1/loyalty/coupons/:code defines (gated loyalty.coupon.issue, re-define refused 409), POST .../redemptions/:id records a redemption (gated loyalty.coupon.redeem) running redeemCoupon against the WHOLE cloud history so a cross-lane double-use a stale offline lane cache missed is refused 409 coupon_limit_reached (visible conflict, hard rule #10) and a same-id re-sync is idempotent 200, POST /v1/loyalty/offers issues only with BOTH profiling+marketing consent else 422 (M16-FR-02), POST /v1/loyalty/referrals/:id pays only on a qualifying purchase / refuses self-referral / never twice; event-sourced CouponIssued/CouponRedeemed/ReferralRewarded, restart-safe; integration-tested 8 cases. RE-RATED PARTIALLY_WIRED->WIRED: every M17 FR is now live on the API."},
    {"id": "M18", "label": "PARTIALLY_WIRED", "evidence": "RTM: lifecycle/reservation/cancel/substitution/channel-reconcile wired; multi-line amendment batching follow-on"},
    {"id": "M19", "label": "PARTIALLY_WIRED", "evidence": "RTM: merchandising PARTIALLY WIRED; packing + dispatch FR-02 now wired on the cloud (API-08: packOrder — weighed-line price captured at packed grams in exact integer minor units, missing/out-of-range cold-chain temperature refused, incompatible crate refused while the rest packs; dispatchOrder over the RECORDED pack — manifest derived from what was packed, unsealed_crate/unresolved_lines/no_pack_recorded refused 409; event-sourced OrderPacked/OrderDispatched restart-safe), tests/integration/fulfilment-packing.test.ts; COD reconciliation FR-04 now wired too (API-08: reconcileCod — matched per order to the paisa, short/over/uncollected/unexpected each a valued exception, card method refused hard rule #3), tests/integration/cod-reconciliation.test.ts; delivery state machine/proof FR-01/03 remain foundation-only"},
    {"id": "M20", "label": "PARTIALLY_WIRED", "evidence": "RTM: guest/cart/checkout wired; privacy-centre DSR lifecycle wired on the cloud (raise/verify/fulfil/erasure-plan + overdue; verified-before-fulfil, erase/minimise/retain honesty, append-only), tests/integration/data-rights.test.ts; the erasure PLAN's execution against real data stores + screen->cloud call remain"},
    {"id": "M21", "label": "PARTIALLY_WIRED", "evidence": "RTM: refund guard/returnable wired; exchanges pending; service-desk SLA clocks FR-04 wired (case lifecycle open/first-response/resolve + two SLA clocks, breached queue), tests/integration/service-cases.test.ts; compensation FR-03 wired (grantCompensation §28 — mandatory reason, agent authority limit, separate approver above it, absolute policy cap; append-only ledger), tests/integration/service-compensation.test.ts; AI-draft approval FR-03 wired (approveDraft P-05 — an AI drafts, a named human sends; no send route exists; approval attributed to the caller not a model; a draft citing no evidence refused; an edit recorded as an edit), tests/integration/service-ai-drafts.test.ts; campaign send-gate FR-01 wired (planCampaign — consent checked PER RECIPIENT at the send against the shop's OWN stored ConsentRecord ledger via mayWeSend (P-02), channel/purpose separate, an unapproved template blocks the whole campaign, a promotion in a transactional message refused, the excluded count always reported by reason; append-only CampaignPlanned decision log, counts-only per PRV), tests/integration/campaign-consent.test.ts; journeys+attribution FR-02 wired (findJourneyCandidates — quiet period so a nudge is not surveillance, stale/completed/min-value filters; measureCampaign — only post-send in-window orders by recipients, control-group conversion reported beside it, no control = activity not uplift), tests/integration/campaign-journeys.test.ts; CSAT FR-04 wired (serviceReport — a customer rates a RESOLVED case, score 1-5 append-only; the report carries CSAT with its RESPONSE RATE, and breaches on both clocks), tests/integration/service-csat.test.ts — the whole service-desk + CRM side of M21 is now wired; the returns/exchanges side (exchanges, no-receipt returns) is owner-DEFERRED to R5 (CH-01, docs/registers/changes.md, 24 Aug 2026) — deferred-but-unbuilt, so M21 stays PARTIALLY_WIRED (headline not inflated by the deferral)"},
    {"id": "M22", "label": "PARTIALLY_WIRED", "evidence": "RTM: credit/collections/commission/docs wired; portal needs external auth"},
    {"id": "M23", "label": "PARTIALLY_WIRED", "externalBlocker": "live GST-return filing + e-invoice/e-way-bill live portal need production credentials + CA/legal sign-off", "evidence": "RTM: credit-notes wired; GST submission safety + e-invoice reconciliation integration-tested; close totals refuse (need second source)"},
    {"id": "M24", "label": "PARTIALLY_WIRED", "evidence": "RTM: submissions/compliance/statement wired; refusal-audit + probe detection wired (PortalActionAudited on every submission outcome, GET /v1/supplier-portal/probing runs findProbing), tests/integration/supplier-portal-probing.test.ts; server-side scoping (FR-01) still engine-only"},
    {"id": "M25", "label": "PARTIALLY_WIRED", "evidence": "RTM: workforce decision surface wired (roster-gaps/task-gate/checklist/incentive/SOP cloud routes) — and labour-cost now wired too (POST /v1/hr/workforce/labour-cost runs labourCost, reported-never-enforced: above-guide is 'worth a look', a no-sales day is not_meaningful, no function can refuse a roster on cost; gated workforce.roster.read, tests/integration/workforce-labour-cost.test.ts 4 cases), so the whole packages/workforce read/decision surface is now on the live API with nothing engine-only left. Rostering/attendance/certification/SOP durable stores + ESS screen remain the follow-on (M25 stays PARTIALLY_WIRED)"},
    {"id": "M26", "label": "WIRED", "evidence": "RTM: WIRED, every FR integration-tested; IoT pending"},
    {"id": "M27", "label": "PARTIALLY_WIRED", "evidence": "RTM: charge/settlement wired (FR-01/03). OWNERSHIP + ELIGIBILITY now WIRED too (M27-WIRE): mayConcessionTrade on GET /v1/concession/contracts/:id/may-trade (FR-04 — lapsed agreement/insurance/licence blocks new sales, every reason at once + expiry warnings; the contract route now captures insuranceUntil/licenceUntil/approvedBy, previously dropped so the gate could never pass); depositPosition (FR-01 — deposit is a liability, unapproved forfeit stays a liability) on POST /v1/concession/concessionaires/:id/deposit-movements/:movementId + GET .../deposit; valueOwnStock (FR-02 — excludes concession/consignment/customer stock and names it) on POST /v1/concession/valuation; checkStockAccess (FR-02 — store staff cannot write off the concession's stock, a concessionaire touching what is not theirs is a security event) on POST /v1/concession/stock-access; event-sourced ConcessionDepositMoved, restart-safe, integration-tested 10 cases. HELD at PARTIALLY_WIRED (conservative): valuation + stock-access compute over SUPPLIED lots and are not yet auto-fed from the live M08 ownership ledger, and the POS-side concession-sale attribution at the till is a separate wire."},
    {"id": "M28", "label": "PARTIALLY_WIRED", "evidence": "RTM: scrap/packaging/reporting/write-off wired; write-off review office screen shipped; approval-threshold controls follow-on"},
    {"id": "M29", "label": "WIRED", "evidence": "RTM: WIRED, KPI figures on tested engine; owner drill-through + KPI comparison FR-02 remainder now wired on the cloud (API-10: drillThrough — scope-enforced, reconciled to the headline with a LOUD discrepancy when the rows do not add up, every drill audited append-only; compareBy — unattributed grouped not dropped, rows reconcile or must not be published), tests/integration/owner-drill-through.test.ts"},
    {"id": "M30", "label": "PARTIALLY_WIRED", "evidence": "RTM: buying PARTIALLY WIRED (engine reused); import job-history + supplier data-quality scoring FR-04 now wired on the cloud (API-03: recordJob/scoreSource/jobHistory/compareSources — refusals kept alongside successes hard rule #6, score belongs to the source, reasons ranked with actions, cost stated in hours a year, list of people to talk to, event-sourced ImportJobRecorded restart-safe), tests/integration/import-quality.test.ts; import validate/preview/commit FR-01/03 + domain export FR-02 remain foundation-only"},
    {"id": "M31", "label": "PARTIALLY_WIRED", "evidence": "RTM: numbering/template-publish/issue/reproduce wired (frozen-content reproduction); batch re-issue + archival follow-on"},
    {"id": "M32", "label": "PARTIALLY_WIRED", "evidence": "RTM: matrix/webhook/mapping wired (FR-01/02/04). MANAGED SECRETS now WIRED too (M32-WIRE, M32-FR-03 / SEC-04 / hard rule #4): the credential lifecycle on API-11 holding NO secret material — POST /v1/integration/secrets/:id registers a vault REFERENCE (a raw value is refused, must be a scheme URI), POST .../rotation rotates WITH an overlap grace (a no-grace rotation refused 409 no_grace_on_rotation — that is a revocation), POST .../revocation revokes immediately and NAMES what breaks, GET /v1/integration/secrets lists the inventory, POST /v1/integration/secrets/review runs reviewSecrets (overdue/never-rotated + blocking revoked-still-referenced / sandbox-in-production, phrased by what each secret protects); gated platform.setup.write/read (owner-scoped); event-sourced SecretStateRecorded, restart-safe, integration-tested 5 cases. HELD at PARTIALLY_WIRED: the connector DELIVERY/dead-letter (FR-02) and usage-signal monitoring (findUsageSignals) remain engine-only worker/transport concerns (network path, not a cloud endpoint)."},
    {"id": "M33", "label": "INTEGRATION_TESTED", "evidence": "RTM: INTEGRATION TESTED, durable API + UI. Device & version control FR-02/04 (A-10) now wired on the cloud too: POST /v1/platform/devices/evaluate runs evaluateDevice (may-trade/must-upgrade/version-killed/unregistered/integrity-failed; a kill deferred to idle so a sale is never interrupted) and POST /v1/platform/devices/fleet-summary runs fleetSummary (trading/blocked/must-upgrade counts, silence as a signal, spread by version); each runs validateVersionPolicy FIRST so a policy that would brick the fleet is refused before deciding; stateless, gated platform.health.read, tests/integration/platform-devices.test.ts 6 cases. And the durable device REGISTRY now built (inc-1 of the screens/write-paths phase): the shop's real fleet is event-sourced on a devices sub-stream and folded latest-per-device (projectFleet, restart-safe) -- POST /v1/platform/devices/:id/register + /:id/status (block/retire/reinstate; status on an unregistered device refused 404) + /:id/report (heartbeat), GET /v1/platform/devices lists the stored fleet, POST /fleet-health runs fleetSummary+evaluateDevice over the STORED fleet (refuses a fleet-bricking policy first); writes gated platform.device.manage, tests/integration/platform-device-registry.test.ts 6 + tests/unit/device-registry-projection.test.ts 6. Fleet-manager SCREEN now built through inc-2b: inc-2a the tested DOM-free session model (apps/web-erp/src/fleet-session.ts, erp-fleet-session.test.ts 7 -- triage by exception P-03, silence-as-warning P-08, icon+word on every tone, register/block/retire affordance gated to platform.device.manage, bilingual); inc-2b the offline shell (apps/web-erp/web/fleet.html + fleet.js wired through edge store-pack/screen-data/screen-server + browser-entry, cached network-first by the shared sw.js with a bilingual stale-copy strip) -- guarded by tests/guardrails/the-devices-screen-is-usable.test.ts 12 + the devices row in every-screen-opens-without-a-network.test.ts + the fleet payload in the-screens-are-fed.test.ts. Write path inc-2c-i now built: the register/block/retire/bring-back action is captured offline-first (apps/web-erp/src/fleet-device-command.ts + a requestDeviceChange method on createFleetSession, tests/unit/erp-fleet-device-command.test.ts 13 + erp-fleet-session.test.ts grown to 16) -- a deterministic deduplicated command committed to the device OUTBOX (never a network call, hard rule #1), guarded against the device's observed status and stamped with the requesting manager. It is a governance command, deliberately NOT relayed under the store token (mirrors GstReturnActionRequested), so its delivery must carry the manager's authority (platform.device.manage + by:<manager>). Shell buttons + browser-entry outbox are inc-2c-ii; the authenticated sync-drain delivery to the registry is inc-2c-iii. M33 stays INTEGRATION_TESTED (delivery of the captured command + the remote-kill write path remain)"},
    {"id": "M34", "label": "PARTIALLY_WIRED", "evidence": "RTM: compliance/retention PARTIALLY WIRED (primitives; FR-03 obligations wired CORE-05); risk-register + gate-blocking FR-04 core wired (open critical risk blocks its QG, accept-in-own-name + mandatory rationale, blockedGates/gateCanPass), tests/integration/risk-register.test.ts; FR-04 follow-on now wired too — incident/remediation/control-health/attestation registers with every link mandatory (recordIncident/recordRemediation/controlHealth/overdueRemediations, event-sourced ControlRegistered/IncidentRecorded/RemediationRecorded/ControlAttested, restart-safe), tests/integration/risk-followthrough.test.ts; audit-trail + retention primitives FR-01/02 remain foundation-only"},
    {"id": "M35", "label": "PARTIALLY_WIRED", "evidence": "RTM: ops/backup/DR PARTIALLY WIRED (restore proven; FR-01 verifier wired); operational health FR-03 + alert-raising FR-04 wired on the cloud (checkHealth canTrade-vs-status, missing=unknown; raiseAlerts owned+deadline), POST /v1/platform/operational-health, tests/integration/operational-health.test.ts; escalation-over-time + edge signal collection remain"},
    {"id": "M36", "label": "PARTIALLY_WIRED", "evidence": "RTM: branding/export/entitlement wired; per-route enforcement follow-on"},
    {"id": "D01", "label": "PARTIALLY_WIRED", "evidence": "recall-block wired; content/images authoring D01-FR-06 NOT STARTED"},
    {"id": "D02", "label": "PARTIALLY_WIRED", "evidence": "merchandising partial; display-funding D02-FR-06 NOT STARTED"},
    {"id": "D03", "label": "PARTIALLY_WIRED", "evidence": "supplier portal wired; landed-cost FR-05 now wired too (POST /v1/purchase/invoices/:id/reconcile apportions freight/duty across the invoice lines by value to the paisa via matchInvoice, tests/integration/purchase-invoice-reconcile.test.ts); OCR/e-invoice import D03-FR-02 NOT STARTED"},
    {"id": "D04", "label": "PARTIALLY_WIRED", "evidence": "POS sale E2E; self-checkout/scan-and-go/price-kiosk DECISIONS now wired on the cloud (POST /v1/self-checkout/assess + /scan-and-go + /kiosk-quote run the tested packages/self-checkout engine -- basket risk scored across the basket with age always a human, scan-and-go release/audit/trust-withdrawn, read-only kiosk that stops quoting when stale; stateless + offline-safe, gated selfcheckout.operate, tests/integration/self-checkout-decisions.test.ts 5 cases); cash/M13/M14/M16 partial"},
    {"id": "D05", "label": "PARTIALLY_WIRED", "evidence": "stock/warehouse/production wired; FEFO/recall partial (M10)"},
    {"id": "D06", "label": "PARTIALLY_WIRED", "evidence": "M05 pricing wired; markdown/competitor-capture partial"},
    {"id": "D07", "label": "PARTIALLY_WIRED", "evidence": "loyalty points/gift wired; coupons/referrals engine-only (M17)"},
    {"id": "D08", "label": "PARTIALLY_WIRED", "evidence": "customer app guest/cart/checkout wired; privacy partial (M20)"},
    {"id": "D09", "label": "PARTIALLY_WIRED", "evidence": "orders/fulfilment/delivery partially wired (M18/M19)"},
    {"id": "D10", "label": "PARTIALLY_WIRED", "evidence": "GST/settlement wired; close control totals refuse (M23)"},
    {"id": "D11", "label": "PARTIALLY_WIRED", "evidence": "facilities wired (M26); workforce partial (M25)"},
    {"id": "D12", "label": "PARTIALLY_WIRED", "evidence": "tenant self-setup integration-tested; admin/retention partial"},
    {"id": "D13", "label": "PARTIALLY_WIRED", "evidence": "reporting wired; unrunnable reports named (M29)"},
    {"id": "D14", "label": "PARTIALLY_WIRED", "evidence": "gateway/webhooks partial; live hardware/ESL/IoT pending"},
    {"id": "A01", "label": "PARTIALLY_WIRED", "evidence": "owner alerts integration-tested; AI brief model not live"},
    {"id": "A02", "label": "PARTIALLY_WIRED", "evidence": "demand-forecast/order-proposal integration-tested; buyer approval"},
    {"id": "A03", "label": "PARTIALLY_WIRED", "evidence": "forecast/markdown/replenishment integration-tested; manager approval"},
    {"id": "A04", "label": "PARTIALLY_WIRED", "evidence": "customer storefront partially wired (M20)"},
    {"id": "A05", "label": "ENGINE_ONLY", "evidence": "service-desk drafting engine-only; no wired AI pipeline"},
    {"id": "A06", "label": "ENGINE_ONLY", "evidence": "ops runbook-recommend engine-only"},
    {"id": "A07", "label": "PARTIALLY_WIRED", "evidence": "fraud-signals integration-tested; detect-only, no autonomous sanctions"},
    {"id": "A08", "label": "ENGINE_ONLY", "evidence": "data-quality detection engine-only"},
    {"id": "A09", "label": "ENGINE_ONLY", "evidence": "marketing draft engine-only"},
    {"id": "A10", "label": "ENGINE_ONLY", "evidence": "workforce/SOP guidance engine-only"},
    {"id": "WF-01", "label": "PARTIALLY_WIRED", "evidence": "spans M03/M04/M05/M30; weakest partially wired"},
    {"id": "WF-02", "label": "PARTIALLY_WIRED", "evidence": "spans M06/M24/M34; partially wired"},
    {"id": "WF-03", "label": "PARTIALLY_WIRED", "evidence": "M06 partial; forecast/order-proposal tested"},
    {"id": "WF-04", "label": "PARTIALLY_WIRED", "evidence": "M07/M10 partial; capture-match/physical-to-system tests"},
    {"id": "WF-05", "label": "PARTIALLY_WIRED", "evidence": "purchase-to-payment tested; M07/M23/M30 partial"},
    {"id": "WF-06", "label": "PARTIALLY_WIRED", "evidence": "replenishment tested; M04/M25 partial"},
    {"id": "WF-07", "label": "PARTIALLY_WIRED", "evidence": "M08 integration-tested, M09 wired; no dedicated e2e"},
    {"id": "WF-08", "label": "PARTIALLY_WIRED", "evidence": "stock count; M23 partial"},
    {"id": "WF-09", "label": "PARTIALLY_WIRED", "evidence": "expiry/recall; M10/M28 partial"},
    {"id": "WF-10", "label": "E2E_VERIFIED", "evidence": "POS sale path M12 E2E verified, offline+sync proven"},
    {"id": "WF-11", "label": "PARTIALLY_WIRED", "evidence": "returns guard wired; M13/M14/M23 partial"},
    {"id": "WF-12", "label": "PARTIALLY_WIRED", "evidence": "day-close-honestly tested; M14/M23 partial"},
    {"id": "WF-13", "label": "PARTIALLY_WIRED", "evidence": "orders-lifecycle tested; M16-M20 partial"},
    {"id": "WF-14", "label": "PARTIALLY_WIRED", "evidence": "pick-to-doorstep tested; M18/M19 partial"},
    {"id": "WF-15", "label": "PARTIALLY_WIRED", "evidence": "delivery; M19/M23 partial"},
    {"id": "WF-16", "label": "PARTIALLY_WIRED", "evidence": "cancel-release wired; M18/M20/M21/M23 partial"},
    {"id": "WF-17", "label": "PARTIALLY_WIRED", "evidence": "service desk M21 partial; M29 wired"},
    {"id": "WF-18", "label": "PARTIALLY_WIRED", "evidence": "finance close M23 partial; M29 wired"},
    {"id": "WF-19", "label": "ENGINE_ONLY", "evidence": "migration engines + cutover runbook only; not run"},
    {"id": "WF-20", "label": "PARTIALLY_WIRED", "evidence": "M33 integration-tested; M35 partial"},
    {"id": "QG-01", "label": "PARTIALLY_WIRED", "evidence": "traceability-integrity guardrail enforces; full gate pre-pilot"},
    {"id": "QG-02", "label": "PARTIALLY_WIRED", "evidence": "screen-usability guardrails; cashier training targets pending UAT"},
    {"id": "QG-03", "label": "PARTIALLY_WIRED", "evidence": "CI tests/lint/secret-scan green; SBOM/pen pending"},
    {"id": "QG-04", "label": "INTEGRATION_TESTED", "evidence": "offline sale+sync proven; screens-open-offline e2e"},
    {"id": "QG-05", "label": "PARTIALLY_WIRED", "evidence": "perf suite exists; certified-hardware p95 pending"},
    {"id": "QG-06", "label": "PARTIALLY_WIRED", "externalBlocker": "independent penetration test not performed (external security vendor)", "evidence": "secure-by-design guardrails (card-data/secrets/shared-login/audit-chain) tested; independent pen test outstanding"},
    {"id": "QG-07", "label": "PARTIALLY_WIRED", "evidence": "reconciliation engines tested; signed control totals need real data"},
    {"id": "QG-08", "label": "PARTIALLY_WIRED", "evidence": "backup-verify+restore proven; full DR rehearsal pending"},
    {"id": "QG-09", "label": "PARTIALLY_WIRED", "evidence": "SOP/curriculum docs; adoption/competency verification pre-pilot"},
    {"id": "QG-10", "label": "PARTIALLY_WIRED", "evidence": "CI smoke/health/rollback; production verification pending"},
    {"id": "QG-11", "label": "PARTIALLY_WIRED", "evidence": "kill-switch/authority/budget integration-tested; model accuracy evals pending"},
    {"id": "QG-12", "label": "PARTIALLY_WIRED", "evidence": "owner decision register/process exists; per-gate evidence ongoing"},
    {"id": "MG-01", "label": "ENGINE_ONLY", "evidence": "discovery framework only; legacy inventory not performed"},
    {"id": "MG-02", "label": "PARTIALLY_WIRED", "evidence": "backup-verification integration-tested (verify/reconcile)"},
    {"id": "MG-03", "label": "PARTIALLY_WIRED", "evidence": "connector-mapping validation integration-tested"},
    {"id": "MG-04", "label": "ENGINE_ONLY", "evidence": "cleaning engine only; no live data run"},
    {"id": "MG-05", "label": "ENGINE_ONLY", "evidence": "trial-load engine only; migration refuses production"},
    {"id": "MG-06", "label": "PARTIALLY_WIRED", "evidence": "reconcile-restore integration-tested; verification report served"},
    {"id": "MG-07", "label": "ENGINE_ONLY", "evidence": "history migration engine only; no legacy data"},
    {"id": "MG-08", "label": "ENGINE_ONLY", "evidence": "opening-state sign-off logic only; not loaded"},
    {"id": "MG-09", "label": "ENGINE_ONLY", "evidence": "delta capture engine only; not run"},
    {"id": "MG-10", "label": "NOT_STARTED", "evidence": "parallel run is a pilot activity; not built"},
    {"id": "MG-11", "label": "PARTIALLY_WIRED", "evidence": "cutover gate guardrail-tested; cutover-weekend runbook complete"},
    {"id": "MG-12", "label": "ENGINE_ONLY", "evidence": "retention/archive engine only; retire post-cutover"}
  ]
}
