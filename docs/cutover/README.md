# `docs/cutover/`

The parallel-run plan and cutover runbook — filled in during Stage 13.

> The migration **design** (§34) is a Stage 4 artifact and lives in
> `../architecture/migration-design.md`. This folder holds the later **operational**
> parallel-run plan and cutover runbook produced at Stage 13.

> This folder is part of the SRE Retail OS repository layout defined in `CLAUDE.md`.
> It is intentionally empty for now — no application code is written during setup.
