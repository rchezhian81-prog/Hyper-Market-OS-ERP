# `docs/roadmap/`

## Baseline control note (7 August 2026)

The owner has designated **v2.1 — "Final Audit-Closed Developer-Ready Baseline"** as the controlling
product baseline. **That file is not yet in this repository** — only `roadmap-v2.0.docx` is present.
This is a document-control gap, tracked as **OA-1** in `docs/OWNER-ACTION-REGISTER.md`. Until the
v2.1 file is provided:

- **v2.0 is used as the working baseline** — the owner has stated v2.1 is the audit-closed successor
  of the *same* scope, so v2.0's 36 modules / 14 extensions / 20 workflows / 10 agents / etc. remain
  authoritative for implementation.
- **No requirement is invented, removed, or altered** on the basis of a v2.1 we have not read. If the
  v2.1 file, once provided, adds or changes any requirement, those deltas become new tracked
  requirements at that point (per change-control, `docs/registers/changes.md`).

`roadmap-v2.0.docx` is retained as superseded historical evidence.

## The controlling document

`roadmap-v2.0.docx` — the **Final Master Roadmap & Developer Requirements v2.0**
(39 sections) — is present here and, pending v2.1 (above), is the **working source of truth** for the
whole project (`CLAUDE.md`).

It defines: 36 modules (M01–M36), 14 developer extensions (D01–D14), 20 workflows
(WF-01–WF-20), 12 quality gates (QG-01–QG-12), 10 AI agents (A01–A10), 12 migration
controls (MG-01–MG-12), releases R0–R8, execution stages 0–19, milestones M0–M8,
owner decisions OD-01–OD-10 and decision fields D1–D8.

- The navigable index of all of the above is in `docs/requirements/index.md`.
- The decisions, risks and compliance obligations are in `docs/registers/`.

> Change control: this document supersedes conflicting earlier statements. No
> requirement may be removed or deferred except through a written, owner-approved
> change (`docs/registers/changes.md`) with a named target release (OD-02, §14, §25).
