// Public surface of @sre/contracts — the shared shapes and primitives every app,
// service and edge component builds against (roadmap §30, P-06). Grows one
// reviewed, tested unit at a time.

export * from './money';
