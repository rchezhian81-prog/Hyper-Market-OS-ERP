// Public surface of @sre/loyalty — money-like loyalty points (M17-FR-01): earn /
// burn / reverse as append-only movements with the balance projected from events,
// an offline cap that prevents double-spend, and a balance that never goes negative.
// Grows one reviewed, tested unit at a time.

export * from './loyalty';
